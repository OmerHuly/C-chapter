/*
The lazy hedgehog starts on top left corner of a 100X100 tiles board.
Each tile has a value between 0 to 1000.
He needs to reach the bottom right corner with the minimanl sum.
The world matrix is in a file -> Needs to be read.
The program prints out the easiest path (i.e. the minimal sum).
*/

#include <stdio.h>

int main()
{
	return 0;
}